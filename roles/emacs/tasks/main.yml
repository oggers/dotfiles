---
# file: roles/emacs/tasks/main.yml
- name: "{{ role_name }} | Checking for Distribution Config: {{ ansible_facts['distribution'] }}"
  ansible.builtin.stat:
    path: "{{ role_path }}/tasks/{{ ansible_facts['distribution'] }}.yml"
  delegate_to: localhost
  register: distribution_config

- name: "{{ role_name }} | Run Tasks: {{ ansible_facts['distribution'] }}"
  ansible.builtin.include_tasks: "{{ ansible_facts['distribution'] }}.yml"
  when: distribution_config.stat.exists

- name: "{{ role_name }} | Execute all tasks as user {{ emacs_user }}"
  become: yes
  become_user: "{{ emacs_user }}"
  block:
    - name: "{{ role_name }} | Check if ruff command exists"
      ansible.builtin.stat:
        path: "{{ user_bin }}/ruff"
      register: ruff_check
      changed_when: false

    - name: "{{ role_name }} | Install ruff"
      ansible.builtin.shell: "{{ user_bin }}/uv tool install ruff"
      when: not ruff_check.stat.exists

    - name: "{{ role_name }} | Check if ty command exists"
      ansible.builtin.stat:
        path: "{{ user_bin }}/ty"
      register: ty_check
      changed_when: false

    - name: "{{ role_name }} | Install ty"
      ansible.builtin.shell: "{{ user_bin }}/uv tool install ty"
      when: not ty_check.stat.exists

    - name: "{{ role_name }} | Link home doom"
      ansible.builtin.file:
        src: "{{ role_path }}/files/doom/"
        dest: "{{ emacs_home }}/.config/doom"
        state: link
      register: result_doom_dir
      when: inventory_hostname == 'localhost'

    - name: "{{ role_name }} | Copy home doom"
      ansible.builtin.copy:
        src: "{{ role_path }}/files/doom"
        dest: "{{ emacs_home }}/.config/"
      register: result_doom_dir
      when: inventory_hostname != 'localhost'

    - name: "{{ role_name }} | Check if .config/emacs exists"
      ansible.builtin.stat:
        path: "{{ emacs_home }}/.config/emacs"
      register: emacs_dir_stat

    - name: "{{ role_name }} | Download doom"
      ansible.builtin.git:
        repo: "https://github.com/doomemacs/doomemacs"
        depth: 1
        dest: "{{ emacs_home }}/.config/emacs"
      register: result_download_doom
      when: not emacs_dir_stat.stat.exists

    - name: "{{ role_name }} | Install doom"
      ansible.builtin.command: "{{ emacs_home }}/.config/emacs/bin/doom --force install"
      when: result_doom_dir.changed

    - name: "{{ role_name }} | Sync doom"
      ansible.builtin.command: "{{ emacs_home }}/.config/emacs/bin/doom sync"
      when: result_doom_dir.changed or result_download_doom.changed

    - name: "{{ role_name }} | Graphical stuff"
      block:
        - name: "{{ role_name }} | Create desktop directory"
          ansible.builtin.file:
            path: "{{ emacs_home }}/.local/share/applications"
            state: directory

        - name: "{{ role_name }} | Install org-protocol desktop file"
          ansible.builtin.template:
            src: "{{ role_path }}/templates/org-protocol.desktop.j2"
            dest: "{{ emacs_home }}/.local/share/applications/org-protocol.desktop"
          register: result_org_protocol

        - name: "{{ role_name }} | Refresh cache database of mime types handled by desktop files"
          ansible.builtin.command: update-desktop-database "{{ emacs_home }}/.local/share/applications"
          when: result_org_protocol.changed

        - name: "{{ role_name }} | Refresh org-protocol handler"
          ansible.builtin.command: xdg-mime default org-protocol.desktop x-scheme-handler/org-protocol
          when: result_org_protocol.changed

        - name: "{{ role_name }} | link org directory"
          ansible.builtin.file:
            src: "{{ emacs_home }}/Sync/{{ item }}"
            dest: "{{ emacs_home }}/{{ item }}"
            force: yes
            state: link
          with_items:
            - org
            - org-roam

      when: "session_type.stdout.lower() not in ['', 'tty']"

    - name: "{{ role_name }} | ~/.emacs and ~/.emacs.d not present"
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      with_items:
        - "{{ emacs_home }}/.emacs"
        - "{{ emacs_home }}/.emacs.d"

    # now I use astral's ty
    # - name: "{{ role_name }} | Check if basedpyright is already installed"
    #   ansible.builtin.command: which basedpyright
    #   register: pyright_installed
    #   ignore_errors: yes
    #   changed_when: false

    # - name: "{{ role_name }} | Install basedpyright"
    #   ansible.builtin.command: uv tool install basedpyright
    #   when: pyright_installed.rc != 0

    - name: "{{ role_name }} | update .zshrc"
      ansible.builtin.blockinfile:
        path: "{{ emacs_home }}/.zshrc"
        prepend_newline: yes
        append_newline: yes
        create: yes
        marker: "# {mark} ANSIBLE MANAGED BLOCK role emacs"
        block: |
          alias e="emacsclient -t -a ''"
          export EDITOR="e"
          export VISUAL="$EDITOR"

    - name: "{{ role_name }} | update .bashrc"
      ansible.builtin.blockinfile:
        path: "{{ emacs_home }}/.bashrc"
        prepend_newline: yes
        append_newline: yes
        create: yes
        marker: "# {mark} ANSIBLE MANAGED BLOCK role emacs"
        block: |
          alias e="emacsclient -t -a ''"
          export EDITOR="e"
          export VISUAL="$EDITOR"

    - name: "{{ role_name }} systemd"
      block:

        - name: Create user systemd directory
          ansible.builtin.file:
            path: "{{ emacs_home }}/.config/systemd/user"
            state: directory
            mode: '0755'

        - name: "{{ role_name }} | Copy unit file"
          ansible.builtin.copy:
            src: files/my-emacs.service
            dest: "{{ emacs_home }}/.config/systemd/user/my-emacs.service"
            mode: '0644'

        - name: "{{ role_name }} | Enable and start emacs server"
          ansible.builtin.systemd_service:
            name: my-emacs
            daemon_reload: true
            enabled: true
            state: started
            scope: user

      when: inventory_hostname == 'localhost'

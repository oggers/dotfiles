---
# file: shell/defaults/main.yml
shell_user: "{{ zsh_user | default(ansible_facts['user_id']) }}"
shell_home: "{{ zsh_home | default(ansible_facts['user_dir']) }}"
shell_zsh_theme: "robbyrussell"
shell_zsh_plugins:
  - git
  - zoxide
  - fzf
  - zsh-autosuggestions
  - zsh-completions
shell_bat_command: "{{ (ansible_facts['os_family'] == 'Debian') | ternary('batcat', 'bat') }}"
